import React from 'react';

const MiddleText = ({ text }) => {
  return (
    <div className="text-center py-5">
      <h2 className="text-4xl font-bold text-primary">{text}</h2>
    </div>
  );
};

export default MiddleText;